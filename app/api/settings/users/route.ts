import bcrypt from "bcryptjs";import {NextResponse} from "next/server";import {getSessionUserId} from "../../../../lib/auth/session";import {recordAuditLog} from "../../../../lib/audit/audit-service";import {prisma} from "../../../../lib/prisma";import type{UserRole}from"../../../../app/generated/prisma/enums";
const roles=new Set(["EMPLOYEE","TEAM_LEAD","MANAGEMENT","ADMIN"]);
async function admin(){const id=await getSessionUserId();if(!id)return null;const user=await prisma.user.findFirst({where:{id,isActive:true,isDeleted:false},select:{id:true,hotelTenantId:true,role:true}});return user?.role==="ADMIN"?user:null}
export async function GET(){const current=await admin();if(!current)return NextResponse.json({error:"FORBIDDEN"},{status:403});const[users,departments]=await Promise.all([prisma.user.findMany({where:{hotelTenantId:current.hotelTenantId,isDeleted:false},orderBy:[{firstName:"asc"},{lastName:"asc"}],select:{id:true,firstName:true,lastName:true,email:true,phoneNumber:true,role:true,isActive:true,departmentId:true,department:{select:{nameEn:true,nameDe:true,nameIt:true,isDeleted:true}}}}),prisma.department.findMany({where:{hotelTenantId:current.hotelTenantId,isDeleted:false,isActive:true},orderBy:{nameEn:"asc"},select:{id:true,nameEn:true,nameDe:true,nameIt:true}})]);return NextResponse.json({currentUserId:current.id,users:users.map(u=>({id:u.id,first_name:u.firstName,last_name:u.lastName,email:u.email,phone_number:u.phoneNumber,role:u.role,is_active:u.isActive,department_id:u.departmentId,department_en:u.department&&!u.department.isDeleted?u.department.nameEn:null,department_de:u.department&&!u.department.isDeleted?u.department.nameDe:null,department_it:u.department&&!u.department.isDeleted?u.department.nameIt:null})),departments:departments.map(d=>({id:d.id,name_en:d.nameEn,name_de:d.nameDe,name_it:d.nameIt}))})}
export async function POST(request:Request){const current=await admin();if(!current)return NextResponse.json({error:"FORBIDDEN"},{status:403});const body=await request.json().catch(()=>null)as Record<string,unknown>|null;const firstName=typeof body?.firstName==="string"?body.firstName.trim():"",lastName=typeof body?.lastName==="string"?body.lastName.trim():"",email=typeof body?.email==="string"?body.email.trim().toLowerCase():"",password=typeof body?.password==="string"?body.password:"",phone=typeof body?.phone==="string"?body.phone.trim()||null:null,role=typeof body?.role==="string"?body.role:"",departmentId=typeof body?.departmentId==="string"&&body.departmentId?body.departmentId:null,isActive=body?.isActive!==false;if(!firstName||!lastName||!/^\S+@\S+\.\S+$/.test(email)||password.length<8||!roles.has(role))return NextResponse.json({error:"INVALID_FIELDS"},{status:400});if(departmentId&&!await prisma.department.findFirst({where:{id:departmentId,hotelTenantId:current.hotelTenantId,isDeleted:false},select:{id:true}}))return NextResponse.json({error:"INVALID_DEPARTMENT"},{status:400});try{await prisma.$transaction(async tx=>{const hotel=await tx.hotelTenant.findUniqueOrThrow({where:{id:current.hotelTenantId},select:{hotelLanguage:true}});const user=await tx.user.create({data:{hotelTenantId:current.hotelTenantId,firstName,lastName,email,passwordHash:await bcrypt.hash(password,12),phoneNumber:phone,language:hotel.hotelLanguage,role:role as UserRole,departmentId,isActive}});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:"CREATE",entityType:"USER",entityId:user.id,changes:{after:{firstName,lastName,email,role}}})});return NextResponse.json({success:true},{status:201})}catch(error){if(error&&typeof error==="object"&&"code"in error&&error.code==="P2002")return NextResponse.json({error:"EMAIL_EXISTS"},{status:409});throw error}}
