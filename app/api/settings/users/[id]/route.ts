import bcrypt from"bcryptjs";import{NextResponse}from"next/server";import{getSessionUserId}from"../../../../../lib/auth/session";import{recordAuditLog}from"../../../../../lib/audit/audit-service";import{prisma}from"../../../../../lib/prisma";import type{UserRole}from"../../../../../app/generated/prisma/enums";
const roles=new Set(["EMPLOYEE","TEAM_LEAD","MANAGEMENT","ADMIN"]);async function admin(){const sessionId=await getSessionUserId();if(!sessionId)return null;const user=await prisma.user.findFirst({where:{id:sessionId,isActive:true,isDeleted:false},select:{id:true,hotelTenantId:true,role:true}});return user?.role==="ADMIN"?user:null}
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const current=await admin();if(!current)return NextResponse.json({error:"FORBIDDEN"},{status:403});const{id}=await params;const body=await request.json().catch(()=>null)as Record<string,unknown>|null;const firstName=typeof body?.firstName==="string"?body.firstName.trim():"",lastName=typeof body?.lastName==="string"?body.lastName.trim():"",email=typeof body?.email==="string"?body.email.trim().toLowerCase():"",password=typeof body?.password==="string"?body.password:"",phone=typeof body?.phone==="string"?body.phone.trim()||null:null,role=typeof body?.role==="string"?body.role:"",departmentId=typeof body?.departmentId==="string"&&body.departmentId?body.departmentId:null,isActive=body?.isActive===true;if(!firstName||!lastName||!/^\S+@\S+\.\S+$/.test(email)||(password&&password.length<8)||!roles.has(role))return NextResponse.json({error:"INVALID_FIELDS"},{status:400});if(id===current.id&&(!isActive||role!=="ADMIN"))return NextResponse.json({error:"CANNOT_RESTRICT_SELF"},{status:400});if(departmentId&&!await prisma.department.findFirst({where:{id:departmentId,hotelTenantId:current.hotelTenantId,isDeleted:false},select:{id:true}}))return NextResponse.json({error:"INVALID_DEPARTMENT"},{status:400});try{const found=await prisma.$transaction(async tx=>{const previous=await tx.user.findFirst({where:{id,hotelTenantId:current.hotelTenantId,isDeleted:false}});if(!previous)return false;await tx.user.update({where:{id},data:{firstName,lastName,email,phoneNumber:phone,role:role as UserRole,departmentId,isActive,...(password?{passwordHash:await bcrypt.hash(password,12)}:{})}});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:previous.isActive!==isActive?"STATUS_CHANGE":"UPDATE",entityType:"USER",entityId:id,changes:{before:{firstName:previous.firstName,lastName:previous.lastName,email:previous.email,role:previous.role,isActive:previous.isActive},after:{firstName,lastName,email,role,isActive}}});return true});if(!found)return NextResponse.json({error:"NOT_FOUND"},{status:404});return NextResponse.json({success:true})}catch(error){if(error&&typeof error==="object"&&"code"in error&&error.code==="P2002")return NextResponse.json({error:"EMAIL_EXISTS"},{status:409});throw error}}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){const current=await admin();if(!current)return NextResponse.json({error:"FORBIDDEN"},{status:403});const{id}=await params;if(id===current.id)return NextResponse.json({error:"CANNOT_DELETE_SELF"},{status:400});const found=await prisma.$transaction(async tx=>{const user=await tx.user.findFirst({where:{id,hotelTenantId:current.hotelTenantId,isDeleted:false}});if(!user)return false;await tx.user.update({where:{id},data:{isDeleted:true,isActive:false,deletedAt:new Date()}});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:"DELETE",entityType:"USER",entityId:id,changes:{before:{firstName:user.firstName,lastName:user.lastName,email:user.email}}});return true});return found?NextResponse.json({success:true}):NextResponse.json({error:"NOT_FOUND"},{status:404})}
