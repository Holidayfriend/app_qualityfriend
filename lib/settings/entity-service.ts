import{NextResponse}from"next/server";import{recordAuditLog}from"../audit/audit-service";import{getSessionUserId}from"../auth/session";import{prisma}from"../prisma";
export type EntityType="department"|"team";type Row={id:string;nameEn:string;nameDe:string;nameIt:string};async function actor(){const id=await getSessionUserId();if(!id)return null;return prisma.user.findFirst({where:{id,isActive:true,isDeleted:false},select:{id:true,hotelTenantId:true}})}function names(body:unknown){if(!body||typeof body!=="object")return null;const data=body as Record<string,unknown>,nameEn=typeof data.nameEn==="string"?data.nameEn.trim():"",nameDe=typeof data.nameDe==="string"?data.nameDe.trim():"",nameIt=typeof data.nameIt==="string"?data.nameIt.trim():"";return nameEn&&nameDe&&nameIt?{nameEn,nameDe,nameIt}:null}function output(row:Row){return{id:row.id,names:{en:row.nameEn,de:row.nameDe,it:row.nameIt},count:0}}async function find(type:EntityType,id:string,hotelTenantId:string){return type==="department"?prisma.department.findFirst({where:{id,hotelTenantId,isDeleted:false},select:{id:true,nameEn:true,nameDe:true,nameIt:true}}):prisma.team.findFirst({where:{id,hotelTenantId,isDeleted:false},select:{id:true,nameEn:true,nameDe:true,nameIt:true}})}
export async function listEntities(type:EntityType){const current=await actor();if(!current)return NextResponse.json({error:"UNAUTHENTICATED"},{status:401});const rows=type==="department"?await prisma.department.findMany({where:{hotelTenantId:current.hotelTenantId,isDeleted:false},orderBy:{nameEn:"asc"},select:{id:true,nameEn:true,nameDe:true,nameIt:true}}):await prisma.team.findMany({where:{hotelTenantId:current.hotelTenantId,isDeleted:false},orderBy:{nameEn:"asc"},select:{id:true,nameEn:true,nameDe:true,nameIt:true}});return NextResponse.json(rows.map(output))}
export async function createEntity(request:Request,type:EntityType){const current=await actor();if(!current)return NextResponse.json({error:"UNAUTHENTICATED"},{status:401});const values=names(await request.json().catch(()=>null));if(!values)return NextResponse.json({error:"INVALID_FIELDS"},{status:400});try{const entity=await prisma.$transaction(async tx=>{const data={hotelTenantId:current.hotelTenantId,...values,createdById:current.id,updatedById:current.id};const created=type==="department"?await tx.department.create({data,select:{id:true,nameEn:true,nameDe:true,nameIt:true}}):await tx.team.create({data,select:{id:true,nameEn:true,nameDe:true,nameIt:true}});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:"CREATE",entityType:type.toUpperCase(),entityId:created.id,changes:{after:output(created).names}});return created});return NextResponse.json(output(entity),{status:201})}catch(error){if(error&&typeof error==="object"&&"code"in error&&error.code==="P2002")return NextResponse.json({error:"NAME_EXISTS"},{status:409});throw error}}
export async function updateEntity(request:Request,type:EntityType,id:string){const current=await actor();if(!current)return NextResponse.json({error:"UNAUTHENTICATED"},{status:401});const values=names(await request.json().catch(()=>null));if(!values)return NextResponse.json({error:"INVALID_FIELDS"},{status:400});try{const entity=await prisma.$transaction(async tx=>{const previous=await find(type,id,current.hotelTenantId);if(!previous)return null;const data={...values,updatedById:current.id};const updated=type==="department"?await tx.department.update({where:{id},data,select:{id:true,nameEn:true,nameDe:true,nameIt:true}}):await tx.team.update({where:{id},data,select:{id:true,nameEn:true,nameDe:true,nameIt:true}});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:"UPDATE",entityType:type.toUpperCase(),entityId:id,changes:{before:output(previous).names,after:output(updated).names}});return updated});if(!entity)return NextResponse.json({error:"NOT_FOUND"},{status:404});return NextResponse.json(output(entity))}catch(error){if(error&&typeof error==="object"&&"code"in error&&error.code==="P2002")return NextResponse.json({error:"NAME_EXISTS"},{status:409});throw error}}
export async function deleteEntity(type:EntityType,id:string){const current=await actor();if(!current)return NextResponse.json({error:"UNAUTHENTICATED"},{status:401});const entity=await prisma.$transaction(async tx=>{const previous=await find(type,id,current.hotelTenantId);if(!previous)return null;const data={isDeleted:true,isActive:false,deletedAt:new Date(),updatedById:current.id};if(type==="department")await tx.department.update({where:{id},data});else await tx.team.update({where:{id},data});await recordAuditLog(tx,{hotelTenantId:current.hotelTenantId,actorId:current.id,action:"DELETE",entityType:type.toUpperCase(),entityId:id,changes:{before:output(previous).names,after:{isDeleted:true,isActive:false}}});return previous});return entity?NextResponse.json({success:true}):NextResponse.json({error:"NOT_FOUND"},{status:404})}
